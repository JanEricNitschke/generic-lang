# Additional utility functions

# Enumerate function that returns a list of [index, value] pairs
fun enumerate(iterable) {
    var result = [];
    var index = 0;
    foreach (var item in iterable) {
        result.append([index, item]);
        index = index + 1;
    }
    return result;
}

# Global version string
var VERSION = "1.0.0";