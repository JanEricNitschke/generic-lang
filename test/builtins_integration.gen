# Test that builtins are available in user programs
var list_values = [true, false, true];
var all_true = all(list_values);
print(all_true); # expect: false

var any_true = any(list_values);
print(any_true); # expect: true

var items = ["first", "second"];
var indexed = enumerate(items);
print(indexed[0][1]); # expect: first

print("Using version: " + VERSION); # expect: Using version: 1.0.0