var iterator = enumerate(["a", "b", "c"]);
print(iterator); # expect: <generator of enumerate>
foreach (var item in iterator) {
    print(item); # expect: (0, a)
                 # expect: (1, b)
                 # expect: (2, c)
}
iterator = enumerate([]);
print(iterator); # expect: <generator of enumerate>
foreach (var item in iterator) {
    print(item);
}

print(List(enumerate(["x", "y", "z"]))); # expect: [(0, x), (1, y), (2, z)]
