# Test enumerate function
var data = ["a", "b", "c"];
var enumerated = enumerate(data);
print(enumerated[0]); # expect: [0, a]
print(enumerated[1]); # expect: [1, b]
print(enumerated[2]); # expect: [2, c]
print(VERSION); # expect: 1.0.0