class Error < Exception {}

class ThrowOnLen {
    __len__() {
        throw Error();
    }
}

try {
    len(ThrowOnLen());
} catch Error {
    print("Caught");  # expect: Caught
}

len(ThrowOnLen());  # expect runtime error: Exception: __str__ method failed with runtime error
