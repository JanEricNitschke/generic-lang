class Error < Exception {}

class ThrowOnLen {
    __len__() {
        throw Error();
    }
}

try {
    len(ThrowOnLen());
} catch Error {
    print("Caught");  # expect: Caught
}

len(ThrowOnLen());  # expect runtime error: TypeError: Native method '__init__' of class Error expected 1 argument, got 0.
