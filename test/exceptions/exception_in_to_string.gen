class Error < Exception {}

class ThrowOnToString {
    __str__() {
        throw Error();
    }
}

try {
    str(ThrowOnToString());
} catch Error {
    print("Caught");  # expect: Caught
}

str(ThrowOnToString());  # expect runtime error: Exception: __str__ method failed with runtime error

