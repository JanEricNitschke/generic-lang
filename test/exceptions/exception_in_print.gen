class Error < Exception {}

class ThrowOnToString {
    __str__() {
        throw Error();
    }
}

try {
    print(ThrowOnToString());
} catch Error {
    print("Caught");  # expect: Caught
}

print(ThrowOnToString());  # expect runtime error: Exception: __str__ method failed with runtime error

