class Error < Exception {}

class ThrowOnHash {
    __eq__() {
        throw Error();
    }
}

try {
    {ThrowOnHash()}
} catch Error {
    print("Caught");  # expect: Caught
} else {
    print("No Exception");
}

{ThrowOnHash()} # expect runtime error: [line 11] Error at '}': Expect ';' after expression.
